import { TDirectiveDirective } from './t-directive.directive';

describe('TDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
